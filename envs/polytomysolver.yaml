name: polytomysolver
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=2.7
  - pip=19.1.1
  - six=1.12.0
  - ete3=3.1.1
  - lxml=4.3.4

  - pip:
    - profileNJ

    #profileNJ triggers the download of the latest numpy version, this forces a compatible version
    - numpy==1.10.4
